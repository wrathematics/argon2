ARGON2_OBJS = \
	argon2/blake2/blake2b.o \
	argon2/argon2.o \
	argon2/core.o \
	argon2/encoding.o \
	argon2/thread.o \
	argon2/ref.o

R_OBJS = argon2_native.o argon2.o blake2.o gen_nonce.o raw_to_char.o

OBJECTS = $(ARGON2_OBJS) $(R_OBJS)

all: $(SHLIB)

$(SHLIB): $(OBJECTS) 


clean:
	rm -rf *.o *.so *.dll 
